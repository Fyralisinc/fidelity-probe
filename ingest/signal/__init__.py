"""Signal (linked-device messaging) ingestion slice — built blind from the
signal-cli JSON-RPC spec (the signal-cli man page + the documented receive
envelope shapes). Signal has no official server API; the only sound integration
is signal-cli in JSON-RPC daemon mode."""
